package com.ferbajoo.testthings.configuration;

//import com.ferbajoo.testthings.interfaces.Foo;
import com.ferbajoo.testthings.models.ClassModel;
import com.mavi.processor.Foo;

import java.lang.annotation.Annotation;
import java.util.ArrayList;
import java.util.List;

/**
 * Created by
 * feuribe on 17/11/2017.
 */

public class Utilities {


    /**
     * @return all class register in list Configuration
     */
    //public static List<ClassModel> getAllClasses() {
    //    List<ClassModel> models = new ArrayList<>();
    //    for (Class aClass : Configuration.all_classes) {
    //        for (Annotation annotation : aClass.getDeclaredAnnotations()) {
    //            if (annotation instanceof Foo) {
    //                Foo foo = (Foo) annotation;
    //                models.add(new ClassModel(foo.name(), foo.value(), foo.drawable()));
    //            }
    //        }
    //    }
    //    return models;
    //}
//
    /**
     * @param aClass class to register in list
     */
    //public static void setClassRegister(Class<?> aClass) {
    //    if (!Configuration.all_classes.contains(aClass))
    //        Configuration.all_classes.add(aClass);
    //}
}
